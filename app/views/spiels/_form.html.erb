<%= form_for(@spiel) do |f| %>
  <% if @spiel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spiel.errors.count, "error") %> prohibited this spiel from being saved:</h2>

      <ul>
      <% @spiel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
