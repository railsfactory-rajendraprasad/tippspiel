
  <div class="group">
    <%= f.label :start_at, t("activerecord.attributes.spiel.start_at", :default => "Start at"), :class => :label %>
    <%= f.datetime_select :start_at, :class => 'datetime_select' %>
  </div>

  <div class="group">
    <%= f.label :team_a_id, t("activerecord.attributes.spiel.team_a_id", :default => "Team a"), :class => :label %>
    <%= f.collection_select :team_a_id, Team.all, :id, :name, :class => 'select_field' %>
  </div>

  <div class="group">
    <%= f.label :team_b_id, t("activerecord.attributes.spiel.team_b_id", :default => "Team b"), :class => :label %>
    <%= f.collection_select :team_b_id, Team.all, :id, :name, :class => 'select_field' %>
  </div>

  <div class="group">
    <%= f.label :result, t("activerecord.attributes.spiel.result", :default => "Result"), :class => :label %>
    <%= f.text_field :result, :class => 'text_field' %>
  </div>
        
<div class="group navform wat-cf">
  <button class="button" type="submit">
    <%= image_tag("web-app-theme/icons/tick.png", :alt => "#{t("web-app-theme.save", :default => "Save")}") %> <%= t("web-app-theme.save", :default => "Save") %>
  </button>
  <span class="text_button_padding"><%= t("web-app-theme.or", :default => "or") %></span>
	<%= link_to t("web-app-theme.cancel", :default => "Cancel"), spiels_path, :class => "text_button_padding link_button" %>
</div>